# Project related
/plans/

# Bun/TypeScript
/node_modules/
/dist/
/bun.lock

# Trekoon shared operational state
# Keep the repo-scoped SQLite DB out of Git. In worktrees this storage is shared
# across checkouts, so committing it would snapshot live machine-local state.
/.trekoon/*
/.opencode/
/.claude/
/.octto/
/docs/plans/*
.DS_Store
